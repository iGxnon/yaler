# yaler client — local dev config
listen      = "127.0.0.1:1080"
server      = "127.0.0.1"
port        = 8443
path        = "/sw"
password    = "testpass"
sni         = "localhost"
skip_verify = true   # self-signed cert in dev
pool_size   = 64
